%h2 Thank you for your order
- @cart.shopping_cart_items.each do |cart_item|
  = cart_item.item.name
  %p Total:
  = @cart.total
  SEK
%br
Rating
= @rating.rating
= form_for [@rating], url: complete_path do |f|
  = f.select(:rating, ['1','2','3','4','5'])
  = f.submit "Rating"
