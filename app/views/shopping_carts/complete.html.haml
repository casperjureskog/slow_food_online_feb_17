%h2 Thank you for your order
- @cart.shopping_cart_items.each do |cart_item|
  = cart_item.item.name
  %p Total:
  = @cart.total
  SEK
%br
Rating
= @rating.rating
= form_tag select_category_path, method: 'post' do |f|
  = f.select(:rating , ['Starter','Main course','Dessert'])
  = f.submit "Add dish", class: 'button expanded'
